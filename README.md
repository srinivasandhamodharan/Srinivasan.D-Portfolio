# Srinivasan D — Portfolio (React + Vite)

This is a small React + Vite project created from the provided static HTML/CSS/JS portfolio.

Getting started (Windows PowerShell):

1. Install dependencies

```powershell
npm install
```

2. Run the dev server

```powershell
npm run dev
```

3. Build for production

```powershell
npm run build
```

Notes:
- The custom cursor and scroll animations have been ported to React using effects.
- If you want to deploy this to GitHub Pages, Netlify or Vercel, build the project and follow the platform instructions.
